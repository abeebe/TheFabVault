# TheFabVault — Homelab bind-mount compose file
#
# Designed for deployment on ct-102 (10.10.5.252) via Portainer git-mode stack.
#
# Volume layout:
#   /app/storage  → /mnt/fabvault/storage  (CIFS, Unraid NAS via PVE host mp= passthrough)
#   /app/data     → /var/lib/fabvault/db   (ext4 local disk — SQLite requires POSIX locking)
#
# Required env vars (set in Portainer stack environment):
#   AUTH_USERNAME, AUTH_PASSWORD, JWT_SECRET, JWT_TTL
#   CORS_ORIGINS, VITE_API_URL (baked into web image at build time)
#   API_PORT (default: 3005), WEB_PORT (default: 8085)
#
# Historical note (2026-07-12, #2078): this file used to also bind-mount
# /mnt/fabvault/storage (the SAME host dir as /app/storage) to
# /imports/1,2,3 for the mount-scan auto-import subsystem, which is how
# a self-import bug produced ~8,934 spurious asset rows on 2026-07-12.
# The mount-scan subsystem has been removed entirely (see git history
# for services/mountImport.ts) and the /imports/N binds + IMPORT_MOUNT_*
# env vars below with it. Loading is done through the interface (browser
# upload + folder-tree project import) only.
#
# See: ~/projects/DOCS/Reports/holt-fabvault-portainer-migration-2026-05-23.md

services:
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    restart: unless-stopped
    init: true
    ports:
      - "${API_PORT:-3005}:3000"
    environment:
      PORT: "3000"
      AUTH_USERNAME: "${AUTH_USERNAME}"
      AUTH_PASSWORD: "${AUTH_PASSWORD}"
      JWT_SECRET: "${JWT_SECRET}"
      JWT_TTL: "${JWT_TTL:-43200}"
      STORAGE_DIR: "/app/storage"
      DATA_DIR: "/app/data"
      CORS_ORIGINS: "${CORS_ORIGINS:-http://localhost}"
    cap_add:
      - SYS_ADMIN
    volumes:
      - /var/lib/fabvault/db:/app/data
      - /mnt/fabvault/storage:/app/storage
    stop_grace_period: 15s
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/health"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 10s

  web:
    build:
      context: ./web
      dockerfile: Dockerfile
      args:
        VITE_API_URL: "${VITE_API_URL:-http://localhost:3000}"
    restart: unless-stopped
    ports:
      - "${WEB_PORT:-8085}:80"
    depends_on:
      api:
        condition: service_healthy
